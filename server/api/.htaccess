RewriteEngine On

# 所有 /api/* 请求统一路由到 index.php
# index.php 自身不重写（避免循环）
RewriteCond %{REQUEST_URI} !/index\.php$
# 将 /api/ 后面的路径作为 __route 参数传递给 index.php
# /api/login.php?id=1  →  index.php?__route=login.php&id=1
RewriteRule ^(.+)$ index.php?__route=$1 [QSA,L]
